module DFDB.DatabaseSpec where

import ClassyPrelude
import Test.Hspec (Spec, describe, it, pending)

spec :: Spec
spec = describe "Database" $ do
  it "creates a table" pending
  it "inserts to a table" pending
  it "selects from a table" pending
  it "drops a table" pending
  it "creates an index" pending
  it "drops an index" pending
